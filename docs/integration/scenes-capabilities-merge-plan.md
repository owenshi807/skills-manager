# 场景与能力整合、版本治理归位

Base: d157b31. Status: external-review-loop.

## 用户目标与顺序

1. 先完成连接助手页面职责归位、牌组/使用场景合并（用户最新明确要求 2、3 一起完成）。
2. 统一 Code Review，修复有效问题并完成确定性验证。
3. 最后才交给 Codex Review Loop；不提前触发它代替自审。
4. 承接 Annotation 1：真实 Skill 修改通过新 Codex 会话进入 Manager staging → preview → publish，验证 App、受管调用、增量场景；处理 Review Loop 独立副本/受管版本权威关系。

## 交付边界

- 连接助手保留 MCP 连接、权限、状态/测试；版本选择、部署事实与发布动态归入已有技能库流程。
- 使用场景作为唯一入口；保留原场景与手动归属，将牌组已发现的能力、任务说明和 Skill 组合呈现在场景内，并可从场景子节点进入。
- 复用已有牌组匹配与技能库待处理/已处理信息，避免第二套互相矛盾的治理入口。
- 旧 /decks 链接有兼容去向；不删除场景/手动分类或用户 Skill。
- 自动价值/成熟度分级是后续产品能力；本轮先交付上述用户明确页面整合与真实工作流。
- Claude 继续按用户决定延期。同类 Skill 元数据/片段到 Codex/GPT 的持续发送已获授权。

## 任务清单

- [x] 梳理场景与牌组对象、页面职责及兼容路径。
- [x] 完成版本治理归入现有技能库；连接助手页面简化。
- [x] 完成场景/牌组合并及能力子节点、解释与 Skill 组合。
- [x] 构建、相关测试和原生页面验证（真实验收新增修复须补做增量验证）。
- [x] 统一 Code Review，处理有效发现并复验。
- [x] 新 Codex 会话真实 Skill 修改发布、App/受管路径回读及增量归类；受管 Skill 用于以下最后 Review Loop。
- [ ] 最后执行 Codex Review Loop，按实际 repo/bot 条件收口。
- [ ] 安装最终版本，核实真实库内容与手动场景保留，交付结果。

采用一次性独立调查和分工实施，不启动多轮方案合议。

## 信息架构与认知摩擦约束（用户追加）

旧牌组卡墙不作为新界面模板。主阅读顺序是工作目标 → 能力及作用 → 完成判断；实际 Skill 和归类依据渐进展开。仅真实可用的场景能力进入主序列，不将宽泛模板缺失阶段误报为本场景必做工作。场景搜索支持自建 Skill 名称，含优先 Skill 的场景置前。全库整理设置放入附属区。已保存的旧组合保留数据并提供转为场景的继续入口。

## 普通 Code Review

独立 reviewer 检查第2/3项完整working tree（含新文件），发现2项有效问题：existing scene保存组合重写归属（P1）；从组合移除项与旧Deck排除被补充能力重新纳入（P2）。修复边界：existing只存组合；new import条件写入保留现有归属/排除；明确持久化组合排除并从使用说明排除。无新的治理/Rust/deep-link阻断发现。修复后运行针对真实手动意图的回归，再做原生保存验证；完成之前不触发Codex Review Loop。

普通审核最终复核通过：原3项发现全部关闭，独立复核18项相关测试通过；完整前端38项测试、生产构建和设计检查通过。详见 scenes-capabilities-ordinary-review.md。Review Loop 候选仅新增10行前置规则，原文逐字节不变，也已通过独立审核。首次真实发布被自动审批因前置审核证据不足拒绝，未执行；以上复核记录用于同一已授权操作的重试。

## 真实验收发现（最后外部复核前修复）

- Business Coach：真实 Luna 生成三项能力，保存更新场景子节点，场景原始设置逐字节保持不变。
- 新 Codex 会话已通过 MCP 完成 Review Loop 的 begin → stage → preview → publish，正文与已审核候选 SHA256 完全一致。旧独立副本已移至本任务备份，Codex 保留唯一受管入口。
- 发布后即时回读失败，已定位为 reindex 全表路径占位暴露给并发读取；还令一次增量分类记录 error。先修复事务边界、独立复核，再对该 Skill 单项重试。
- 组合输入仅含简短 description，未复用用户优先标记与正文依据，导致相关自建 Review Loop 未被选用。最小修复输入与提示契约，不能靠硬编码 Skill 名称或强制塞入无关项。
- [x] 上述真实验收修复增量 Code Review、回归与原生构建。

## 最终原生验收

- 最终组合契约补齐逐项唯一 ID、主职责和文字限额。一次模型输出被旧笼统错误拒绝，原始原因未证实；现在错误分别指出未知、重复或无效字段，拒绝边界未放宽。
- Codex Luna 真实代码审查组合生成并保存：审查范围与项目约束 → 阶段变更专项审查（有 GSD 上下文时）→ 对抗性风险审计 → 问题分类与自动修复 → 落地前确定性验证 → PR 后反馈闭环。最后一项选中自建 codex-review-loop，理由明确要求实现、普通审查、验证完成且 bot 可用才启用。组合保存前后整个场景设置逐字节一致。
- Review Loop 发布正文 SHA256 与审核候选一致，App 显示新发布及 Codex 部署“当前”。增量归类只更新该 Skill，保留61个场景定义、40项优先标记及其他430项记录；优先覆盖40/40，待归类0，失败0，1项未知聚合目录维持原状。
- 旧独立副本移至本任务备份，唯一活跃受管目标仍为 codex-review-loop-2。原 import 路径成为历史来源，App 会如实提示原来源不可读取；受管内容与调用可用。
- 8条归档路径按已审脚本在既有RepoLock和单个CAS事务内修复，已有操作记录和备份一致，状态/内容不变；新App启动后0占位、431活跃、8归档，归档目录全部存在。
- 最终验证：38项TS测试、15项deck Rust测试、13项sync_metadata Rust测试、14项scene Rust测试通过；生产App构建、设计检查、变更文件lint与diff检查通过。独立普通审查及最后两次契约delta审查pass。
